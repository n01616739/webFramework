<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error</title>
    <!-- Add your CSS files -->
</head>
<body>
    <div class="container text-center mt-5">
        <h1>Error</h1>
        <p class="lead"><%= message %></p>

        <% if (typeof error !== 'undefined' && error) { %> <!-- Check if error is defined -->
            <pre class="bg-light p-3 rounded text-start"><%= error %></pre>
        <% } else { %>
            <p>No additional details are available.</p>
        <% } %>
    </div>
</body>
</html>
